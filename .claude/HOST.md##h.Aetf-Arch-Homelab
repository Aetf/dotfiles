# 本机：Aetf-Arch-Homelab

本文件由 yadm 按主机名分发，是"我跑在哪台机器上"的静态答案，不需要再跑 `hostnamectl` 确认。若内容与实际明显不符（新机器、alt 未生效），才去核实。

- **角色**：家庭服务器，Arch Linux（LTS 内核），ASUS ROG Maximus Z690 Hero。无桌面、无 tty。Claude 常驻此机，绝大多数运维会话在这里跑。详见 [[homelab-server-overview]]。
- **无 sudo**：连 `sudo -n true` 这类探测都会累计 pam_faillock 并锁住用户账号。一切 root 操作给用户命令、自己不执行。见 [[no-sudo-access]]。
- **vault**：`~/.local/state/claude/vault` → `/mnt/nas/Sync/Notes`。
- **可达的其它机器**：`ssh xps`（Aetf-Arch-XPS 笔记本，KDE 桌面）、`ssh archvps`（k3s 另一节点，**远端 shell 是 fish**）、`ssh haos`（Home Assistant OS）。
- **桌面类事实不属于本机**：KDE/Plasma、yakuake/konsole、refind、睡眠/休眠、paru packages.local 全部是 XPS 的事实。记忆索引里带【XPS】标注的条目在本机不成立，本机的配置文件也不能拿来代答 XPS 的问题（yadm 共享 ≠ 两边一致）。
