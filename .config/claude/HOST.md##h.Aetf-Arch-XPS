# 本机：Aetf-Arch-XPS

本文件由 yadm 按主机名分发，是"我跑在哪台机器上"的静态答案，不必再跑 `hostnamectl` 确认。只有内容与实际明显不符（新机器、alt 未生效）才去核实。

- **角色**：Dell XPS 17 9700 笔记本，Arch Linux + KDE Plasma，用户的日常交互机。yakuake/konsole、refind、睡眠/休眠、paru `~/packages.local` 都是本机的事实。
- **不能用 sudo**：用户在 wheel 组，但 Claude 的会话没有 tty，密码提示必然失败。要 root 的操作一律给用户命令，自己不试。
- **vault**：`~/.local/state/claude/vault` → `~/sync/Notes`（Syncthing 源端）。
- **ssh**：`~/.ssh/config` 与 `~/.ssh/config.d/` 里列出的别名都可用——`gw`、`homelab`/`homelabzt`、`haos`、`archvps`、`rpi`、`3dp`、`github`、`xps`/`laptop`（本机自己）。
- **ssh 认证靠 agent**：本机没有专属私钥，所有 ssh 都依赖 `ssh-agent`（见 [[ssh-agent-forwarding-architecture]]）。通常不成问题——用 XPS 时用户一般在场，agent 是活的。agent 不可用时不要绕，直接说明。
- **服务器类事实不属于本机**：k3s/kluster、ZFS `nas` 池、qBittorrent/Jellyfin/immich、UPS、aconfmgr 的 homelab 侧都在 Aetf-Arch-Homelab 上。记忆索引里绝大多数 `homelab-*`/`kluster-*` 条目在本机不成立，要动它们必须 `ssh homelab`、在那一端取证。
