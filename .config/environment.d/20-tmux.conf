# tmux server socket lives in the runtime dir instead of /tmp, so sessions
# launched with a private /tmp (claude-session) can still reach it and
# /tmp cleanup never removes it. Takes effect on the next tmux server start.
TMUX_TMPDIR=${XDG_RUNTIME_DIR}
