server {
	listen 80;
	listen [::]:80;
	server_name maps.unlimitedcodeworks.xyz maps.unlimited-code.works;

	return 301 https://$host$request_uri;
}

server {
	listen 443 ssl http2;
	listen [::]:443 ssl http2;
	server_name maps.unlimitedcodeworks.xyz maps.unlimited-code.works;

	root /srv/http/mc-map-overview;

	access_log /var/log/nginx/sites/mcmap.access netdata;
	error_log /var/log/nginx/sites/mcmap.error error;

	location / {
	}

	#error_page 404 /404.html;

	# redirect server error pages to the static page /50x.html
	#
	#error_page 500 502 503 504 /50x.html;
	#location = /50x.html {
	#	root /usr/share/nginx/html;
	#}
}
