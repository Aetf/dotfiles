server {
	listen 80;
	listen [::]:80;
	server_name dav.unlimited-code.works dav.unlimitedcodeworks.xyz;

	return 301 https://$server_name$request_uri;
}

server {
	listen 443 ssl http2;
	listen [::]:443 ssl http2;

	server_name dav.unlimited-code.works dav.unlimitedcodeworks.xyz;

	access_log /var/log/nginx/sites/webdav.access netdata;
	error_log /var/log/nginx/sites/webdav.error error;
	rewrite_log on;

	ssl_certificate /etc/letsencrypt/live/unlimited-code.works/fullchain.pem;
	ssl_certificate_key /etc/letsencrypt/live/unlimited-code.works/privkey.pem;

    # auth_basic "ArchVPS WebDAV Authentication Required";

	#location / {
	#	root /srv/http/webdav;

	#	# enable creating directories without trailing slash
	#	# needed for macOS client
	#	set $x $uri$request_method;
	#	if ($x ~ [^/]MKCOL$) {
	#		rewrite ^(.*)$ $1/;
	#	}

	#	dav_access user:rw group:rw all:r;
	#	dav_methods PUT DELETE MKCOL COPY MOVE;
	#	dav_ext_methods PROPFIND OPTIONS;
	#	dav_ext_lock zone=archvps;
	#	create_full_put_path on;

	#	client_max_body_size 0;
	#	client_body_temp_path /srv/http/tmp-body;
	#}

    location /dav.php/ {
        # include details about the original request
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Original-Scheme $scheme;
        proxy_set_header X-Forwarded-Proto $scheme;

		# forward to filerun server
        proxy_set_header Host files.unlimited-code.works;
        proxy_pass https://localhost:443/dav.php/;
    }

    #location / {
    #    # include details about the original request
    #    proxy_set_header X-Real-IP $remote_addr;
    #    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #    proxy_set_header X-Original-Scheme $scheme;
    #    proxy_set_header X-Forwarded-Proto $scheme;

	#	# forward to filerun server
    #    proxy_set_header Host files.unlimited-code.works;
    #    proxy_pass https://localhost:443/dav.php/;
    #}

	# redirect server error pages to the static page /50x.html
	#
	#error_page 500 502 503 504 /50x.html;
	#location = /50x.html {
	#	root /usr/share/nginx/html;
	#}
}
