# IME settings
#handled automatically in /etc/X11/Xsession.d/72sogoupinyin

# Default editor
export EDITOR=vim
export VISUAL=$EDITOR

# Used for virtualbox
export VBOX_USB=usbfs

# Predefined variables to Java runtime
#export _JAVA_OPTIONS="-Dawt.useSystemAAFontSettings=on -Dswing.aatext=true"

# Ccache directory
export CCACHE_DIR=$HOME/.cache/ccache
export CCACHE_COMPRESS

# CUDA directory
export CUDA_DIR=/opt/cuda
export CUDA_HOME=$CUDA_DIR

# XDG variables
export XDG_CONFIG_HOME="$HOME/.config"

# Jupyter lab app dir
export JUPYTERLAB_DIR=$HOME/.jupyter/lab/app

# More PATH
## Prepend user's bin directory to PATH
PATH="$HOME/customizations/scripts:$PATH"
## Local bin directory
PATH="$HOME/.local/bin:$PATH"
## Node.js executable
if [ -d $HOME/.node_modules/bin ]; then
    PATH="$PATH:$HOME/.node_modules/bin"
fi
## ruby gem executable
for p in ~/.gem/ruby/*/bin; do
    PATH="$PATH:$p"
done
## rust cargo executable
if [ -d $HOME/.cargo/bin ]; then
    PATH="$PATH:$HOME/.cargo/bin"
fi
## Spack executable
if [ -d $HOME/.local/spack/bin ]; then
    PATH=$"$HOME/.local/spack/bin:$PATH"
fi

export PATH
